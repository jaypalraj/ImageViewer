                if (lblInstruction.Text.ToLower().Contains("skip next"))
                {
                    var noToSkip = skipVal == 0 ? new Random().Next(3, 12) : skipVal;

                    lblInstruction.Text = $"SKIP NEXT {noToSkip}";
                    skipVal = noToSkip;
                    skipVal--;
                }
                else
                    lblInstruction.Text = instructions.ElementAt(randomInstruction.Next(instructions.Count()))?.ToUpper();









Helper.cs

        public static List<string> ReadInstructions()
        {
            int counter = 0;
            string line;
            List<string> instructions = new List<string>();

            StreamReader file = new StreamReader(@"c:\instructions.txt");
            while ((line = file.ReadLine()) != null)
            {
                instructions.Add(line);
                counter++;
            }

            file.Close();

            return instructions;
        }




Add another button to upload file to priority list, the folder should open to the location of the folder from the file selected




        private void btnAddToPriority_Click(object sender, EventArgs e)
        {
            var selectedItem = lstBoxFiles.SelectedItem?.ToString();
            var fileInfo = new FileInfo(selectedItem);

            using (OpenFileDialog openFileDialog = new OpenFileDialog())
            {
                openFileDialog.InitialDirectory = fileInfo.DirectoryName;
                openFileDialog.Filter = "txt files (*.txt)|*.txt|All files (*.*)|*.*";
                openFileDialog.FilterIndex = 2;
                openFileDialog.RestoreDirectory = true;

                if (openFileDialog.ShowDialog() == DialogResult.OK)
                {
                    //Get the path of specified file
                    var filePath = openFileDialog.FileName;
                    listPriority = listPriority ?? new List<string>();
                    listPriority.Add(filePath);

                    lstBoxPriority.Items.Add(filePath);

                }
            }
        }



https://stackoverflow.com/questions/8347585/can-openfiledialog-automatically-select-the-file-with-the-value-set-in-filename
